meta:
  name: auto14
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  persistence:
    - mountPath: /app/prisma
      name: prisma-db # Persistence for SQLite (if used) or logs
