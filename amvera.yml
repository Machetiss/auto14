meta:
  name: auto14
  toolchain:
    name: docker # Fixed based on Amvera error: 'meta.toolchain' must be present

build:
  dockerfile: Dockerfile

run:
  persistence:
    - mountPath: /app/prisma
      name: prisma-db # Persistence for SQLite (if used) or logs
